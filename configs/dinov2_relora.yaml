experiment:
  name: dinov2-relora
  name_suffix: relora-r2
  seed: 42
  device: auto
  output_dir: ./runs

model:
  name: dinov2_relora
  params:
    model_name: facebook/dinov2-small
    dropout: 0.0
    relora:
      r: 2
      alpha: 4
      dropout: 0.01
      target_modules: ["query", "key", "value", "dense", "fc1", "fc2"]
      merge_interval: 200
      warm_start_steps: 0
      adapter_warmup_steps: 50
      freeze_base: true
      prune_b_state: true
    relora_optimizer:
      lr: 3.0e-4
      weight_decay: 0.0

training:
  epochs: 4
  gradient_clip_norm: 0.0
  grad_accumulation: 1
  log_every: 1
  eval_every: 1
  save_every: 1
  precision:
    enabled: true
    dtype: bf16
    grad_scaler: false
  optimizer:
    type: adamw
    lr: 0.0003
    weight_decay: 0.0
    betas: [0.9, 0.999]
    eps: 0.00000001
  scheduler:
    type: cosine
    warmup: 0.1
    min_lr_mult: 0.1
  monitor:
    grad_norm: true
    param_norm: true
    memory: true

data:
  root: /speedy/datasets
  image_size: 224
  batch_size: 256
  num_workers: 8
  pin_memory: true
  persistent_workers: true
  normalization: dinov2
  augment: true
  drop_last: true

logging:
  use_wandb: true
  project: fisher-relora
  entity: null
  run_name: null
  group: null
  tags: ["dinov2", "relora"]
  notes: null

datasets:
  - name: ImageNet
    val_split: test
    normalization: dinov2
